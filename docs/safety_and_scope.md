# Safety and Scope

This project provides writing workflow Skills.

It does not:

- impersonate creators
- include private transcripts
- bypass user review
- automatically publish generated text
- run benchmarks by default
- automatically promote outputs into a golden set
- guarantee that outputs match any person exactly

